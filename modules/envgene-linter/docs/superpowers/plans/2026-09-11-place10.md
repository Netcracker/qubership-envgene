# PLACE-10 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Track steps with checkboxes.

**Goal:** Check the directory of each connected typed entity directly.

**Architecture:** Extend shared selection with explicit Shared Template Variable bindings, then use one type/directory predicate for five types. Preserve independent diagnostics from other rules.

**Tech Stack:** Existing Python, ruamel.yaml, pytest, Click.

**Spec:** docs/superpowers/specs/2026-09-11-place10-design.md

## Global Constraints

Only connected objects. Exact directory table from spec. Warning/Fix, exit0. No overlap suppression, missing-reference findings, content inference, autofix, network, secret output, real corpus writes. Controller owns docs/Git; preserve unrelated untracked files.

### Task 1: Implement PLACE-10 and document it

**Files:** create rules/place10.py and tests/test_place10.py; modify model.py/discovery.py/connections.py for default-empty STV binding/selection, engine.py/report.py/rulemeta.py, affectedintegrationtests. All source under src/envgene_linter/. Controller owns docs/algorithms/place10.md +ru, sharedscope/order docs.

**Interfaces:** `check(index, connections=None) -> list[Finding]`; add shared_template_variables physicalpath set to Connections and shared_template_variable_bindings toEnvModel. Existing APIs remain compatible via defaults.

- [x] Add behavioral tests from spec. Minimal RED:

```python
def test_selected_legacy_profile_directory(repo):
    repo.env("c", "e")
    inventory = repo.root / "environments/c/e/Inventory"
    with (inventory / "env_definition.yml").open("a") as stream:
        stream.write("  envSpecificResourceProfiles: {cloud: p}\n")
    directory = inventory / "rp_override"
    directory.mkdir()
    (directory / "p.yml").write_text("{}\n")
    findings = check(build_index(repo.root))
    assert len(findings) == 1
    assert findings[0].path == directory / "p.yml"
```

- [x] Run `.venv/bin/python -m pytest tests/test_place10.py -q` and record expected RED.
- [x] Add STV binding discovery and exact local-generator lookup with root/.git safety. Add selected paths to per-env and global connection sets.
- [x] Implement type table and actual-scope directory predicate; dedup physical/type; create Warning/Fix without reading contents.
- [x] Integrate PLACE10 afterPLACE9 in engine/catalog/report; update exact snapshots and add behavioral CLI/HTML tests.
- [x] Run focused selection/discovery/PLACE10/report/CLI tests, then full pytest; compileall/diffcheck. Write self-review report with commands/counts/concerns, freeze. No worker commits/subagents.
- [x] Controller writes EN/RU processing-flow docs and verifies links; read-only25repo smoke.
- [x] Task review and final review, address findings, localcommit.
