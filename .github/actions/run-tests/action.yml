name: 'Run Tests'
description: 'Run Qubership Envgene tests'

inputs:
  python-version:
    description: 'Python version to use for tests'
    required: false
    default: '3.12'

runs:
  using: composite
  steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Configure package sources
      shell: bash
      run: |
        if [ -f build_envgene/build/pip.conf ]; then
          mkdir -p /etc/pip
          cp build_envgene/build/pip.conf /etc/pip.conf
        fi

    - name: Install system dependencies
      shell: bash
      run: |
        apt-get update && apt-get install -y --no-install-recommends \
            build-essential \
            curl \
            git \
            && rm -rf /var/lib/apt/lists/*

        curl -LO https://github.com/getsops/sops/releases/download/v3.9.0/sops-v3.9.0.linux.amd64
        mv sops-v3.9.0.linux.amd64 /usr/local/bin/sops
        chmod +x /usr/local/bin/sops

    - name: Install Python dependencies
      shell: bash
      run: |
        pip install --no-cache-dir --upgrade pip "setuptools==81" wheel
        pip install --no-cache-dir -r build_envgene/build/requirements.txt
        pip install --no-cache-dir -e "modules/envgene[dev]"
        pip install --no-cache-dir -e "modules/artifact-searcher[dev]"
        pip install --no-cache-dir -e "modules/external-cred-provision"
        pip install --no-cache-dir -e "modules/dpg"

    - name: Configure test environment
      shell: bash
      run: |
        echo "PYTHONPATH=${GITHUB_WORKSPACE}:${GITHUB_WORKSPACE}/scripts" >> "${GITHUB_ENV}"

    - name: ENVGENE HELPER test
      shell: bash
      run: |
        cd modules/envgene/envgenehelper
        pytest --capture=no -W ignore::DeprecationWarning --junitxml=../../../junit_envgenehelper.xml
        cd ../../..

    - name: ARTIFACT SEARCHER test
      shell: bash
      run: |
        cd modules/artifact-searcher/artifact_searcher
        pytest --capture=no -W ignore::DeprecationWarning --junitxml=../../../junit_artifact_searcher.xml
        cd ../../..

    - name: EXTERNAL CRED PROVISION test
      shell: bash
      run: |
        cd modules/external-cred-provision
        pytest --capture=no -W ignore::DeprecationWarning --junitxml=../../junit.xml
        cd ../..
        mv junit.xml junit_external_cred_provision.xml

    - name: ENVGENE SCRIPTS SCOPE tests
      shell: bash
      env:
        FULL_ENV_NAME: "sdp-dev/env-1"
        BG_STATE: ""
      run: |
        pytest scripts/tests/ --capture=no -W ignore::DeprecationWarning --junitxml=junit_scripts.xml

    - name: Merge test results
      shell: bash
      run: |
        junitparser merge junit_*.xml junit.xml

    - name: Upload test results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: test-results
        path: junit.xml

    - name: Upload test environments
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: test_artifact
        path: tmp/
