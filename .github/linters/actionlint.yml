# actionlint reports every ShellCheck finding as an error. Ignore non-error
# severities so only ShellCheck `error` fails the lint.
paths:
  "**/*.{yml,yaml}":
    ignore:
      - 'shellcheck reported issue in this script: SC[0-9]+:(warning|info|style):.+'
