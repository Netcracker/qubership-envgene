app-db-cred:
  type: external
  create: true
  secretStore: default_store
  remoteRefPath: "{{ current_env.cloud }}/db"
  properties:
    - name: username
    - name: password
app-sidecar-token:
  type: external
  create: true
  secretStore: default_store
  remoteRefPath: "{{ current_env.name }}/sidecar"
ns-deploy-cred:
  type: external
  create: true
  secretStore: default_store
  remoteRefPath: "{{ current_env.cloud }}"
tenant-cred:
  type: external
  create: true
  secretStore: default_store
