name: "test-env-app"
credentialsId: ""
isServerSideMerge: false
labels: []
cleanInstallApprovalRequired: false
mergeDeployParametersAndE2EParameters: false
deployParameters:
  # Source: nested mapping structure
  database_config:
    connection:
      host: db.example.com
      port: 5432
  # Source: literal block scalar string
  yaml_template: |
    services:
      api:
        image: api:latest
        ports:
          - 8080:8080
  # References to complex structures — must preserve original structure/format
  api_config: ${database_config}
  rendered_template: ${yaml_template}
e2eParameters: {}
technicalConfigurationParameters: {}
deployParameterSets: []
e2eParameterSets: []
technicalConfigurationParameterSets: []
