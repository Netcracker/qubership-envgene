app-db-cred:
  type: external
  create: true
  secretStore: default_store
  remoteRefPath: "test-cluster/test-env/db"
  properties:
    - name: username
    - name: password
local-admin-cred:
  type: usernamePassword
  data:
    username: admin
    password: secret123
