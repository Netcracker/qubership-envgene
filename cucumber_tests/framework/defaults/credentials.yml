# Default credentials used by all tests.
# Place this file inside a test data directory (configuration/credentials/credentials.yml)
# to override these defaults for a specific test case.
test-registry:
  data:
    username: dummy-user
    password: dummy-password
    secret: dummy-secret-value
