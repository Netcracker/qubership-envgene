# Migrate Instance Repository to External Credentials

- [Description](#description)
- [Prerequisites](#prerequisites)
- [Step 1. Run preflight](#step-1-run-preflight)
- [Step 2. Inventory the Instance Repository](#step-2-inventory-the-instance-repository)
- [Step 3. Decide create and remoteRefPath](#step-3-decide-create-and-remoterefpath)
- [Step 4. Configure Secret Store](#step-4-configure-secret-store)
- [Step 5. Convert Cloud Passport Credentials and main file](#step-5-convert-cloud-passport-credentials-and-main-file)
- [Step 6. Convert Shared Credentials](#step-6-convert-shared-credentials)
- [Step 7. Convert System Credentials](#step-7-convert-system-credentials)
- [Step 8. Update environment-specific parameters](#step-8-update-environment-specific-parameters)
- [Step 9. Remove stale and out-of-scope Credential files](#step-9-remove-stale-and-out-of-scope-credential-files)
- [Step 10. Validate before the pipeline](#step-10-validate-before-the-pipeline)
- [Step 11. Run the Instance pipeline](#step-11-run-the-instance-pipeline)
- [Step 12. Verify the generated result](#step-12-verify-the-generated-result)
- [Step 13. Run a test deployment](#step-13-run-a-test-deployment)
- [Rollback](#rollback)
- [See also](#see-also)

## Description

Migrate the Instance Repository from local Credentials to External Credentials.

Complete
[Migrate Template Repository](migrate-template-repository.md)
and publish a concrete Template version before starting here.

Do not remove `data` from any Credential YAML before its real value is already in the Secret
Store. After cutover, EnvGene no longer reads values from Git. If `create: true` is set and the
secret is missing, EnvGene generates a new value - the previous password, token, or key is lost.

Do not put secret values in Git, Merge Requests, or migration notes at any point.

Migration processes **source** Credential files only. Generated
`environments/<cluster>/<env>/Credentials/credentials.yml` files are deleted during cutover and
regenerated by EnvGene post-migration. Deployer credentials (`app-deployer/deployer-creds.yml`) are
out of scope for No-CMDB repositories and are deleted, not converted.

Use one Secret Store per repository (`default_store` in `/configuration/secret-stores.yml`).
Credential entries must set `secretStore` explicitly (usually `default_store`). JSON Schema
documents a default, but the Effective Set calculator reads the field as-is with no runtime
fallback.

### Instance rules

- After merge, every Environment Instance must use External Credentials exclusively. Mixing
  `type: external` with `type: usernamePassword` or `type: secret` in one Environment Instance is
  not supported.
- On duplicate `credId`, precedence is lowest to highest: Credential Template, then Cloud Passport,
  then Shared Credentials.
- `$type: credRef` works only in `deployParameters`, `e2eParameters`, and ParameterSets that feed
  those blocks. It does not work in `technicalConfigurationParameters`.
- Built-in string fields keep a plain `credId` string.
- CMDB import is not supported for Environment Instances with External Credentials.

## Prerequisites

Confirm before you start:

- the Instance Repository uses the No-CMDB approach
- EnvGene is upgraded to a version that supports External Credentials
- a concrete Template version with `external_credential_template` is published
- `/configuration/secret-stores.yml` defines a single `default_store` (Step 4)
- CI/CD authentication variables for that store are configured
- for each Credential you recorded migration-plan decisions: `remoteRefPath`, `create`, and
  `writeToStore` (Instance only). For `writeToStore: true`, transfer secrets to the Secret Store (see
  [Transfer secrets to the Secret Store](transfer-secrets-to-store.md)) before you remove `data` from
  Git
- for each Credential you know who creates the value (EnvGene, pre-existing, or provider) and the
  Store path prefix before you remove `data`

## Step 1. Run preflight

From the Instance Repository root (or via the migration skill scripts directory), run:

```bash
python docs/migrate-to-external-credentials/skills/instance-repository/scripts/preflight.py --repo .
```

The check is read-only. It starts from every `env_definition.yml` and builds the used credential
graph (Cloud Passport, Shared Credentials, System Credentials, ParameterSets, consumers).

It reports blockers such as:

- `sharedMasterCredentialFiles` entries that include a `.yml` extension
- Shared or Passport files referenced but missing on disk
- orphan Shared / Passport cred files not bound from any `env_definition`
- composite credential macros
- `credId` referenced but not declared in an in-scope source
- unsupported credential types
- missing `default_store` in `configuration/secret-stores.yml`

Orphan files are **not** deleted automatically. Clean them up manually (or bind them), then re-run.
Exit code `2` means do not convert yet.

**Result:** preflight status `ok` (warnings may remain for review).

## Step 2. Inventory the Instance Repository

Find all Environment Instances at:

```text
environments/<cluster>/<env>/Inventory/env_definition.yml
```

For each Environment Instance, record:

- `inventory.cloudPassport` - which Cloud Passport it uses
- `envTemplate.sharedMasterCredentialFiles` - which Shared Credential files it binds
- environment-specific ParameterSets
- all `credId` values referenced

Also list every Credential file:

| Location                                                         | What it contains                           |
|------------------------------------------------------------------|--------------------------------------------|
| `environments/<cluster>/cloud-passport/*-creds.yml`              | Cloud Passport Credentials                 |
| `environments/<cluster>/cloud-passport/*.yml`                      | Cloud Passport main files                  |
| `environments/credentials/*.yml`                                 | Repository-level Shared Credentials        |
| `environments/<cluster>/credentials/*.yml`                         | Cluster-level Shared Credentials           |
| `environments/<cluster>/<env>/Inventory/credentials/*.yml`       | Environment-level Shared Credentials       |
| `/configuration/credentials/credentials.yml`                     | System Credentials (git token, registry)   |
| `environments/<cluster>/<env>/Credentials/credentials.yml`       | Generated - do not edit. Delete in Step 9 |
| `environments/<cluster>/app-deployer/deployer-creds.yml`         | Out of scope (No-CMDB). Delete in Step 9   |

Some repositories use `environments/<cluster>/shared-credentials/` instead of
`environments/<cluster>/credentials/`. Treat both as cluster-level Shared Credential sources.

For each local Credential (`type: usernamePassword` or `type: secret`), record:

- `credId`
- source file
- whether `data` contains a real value or a stub (`envgeneNullValue` / empty)

Orphaned Shared Credential files (every `credId` in the file is missing from all consumer
references) are unused. Delete them in Step 9 rather than converting.

**Result:** full inventory of Environment Instances, Credential sources, and per-`credId` values.

## Step 3. Decide create and remoteRefPath

For every local Credential from the inventory, decide in this order:

1. **Scope** - cluster (Cloud Passport), environment, shared across environments, system, or
   provider.
2. **Who creates the value** - EnvGene, pre-existing, or provider. If unclear, stop and confirm.
3. **`create`** - see the matrix.
4. **`remoteRefPath`** - prefix only. EnvGene appends the normalised `credId`.
5. **Confirm ambiguous cases** before you convert YAML or delete `data`.

### Decision matrix

| Who creates the value | Migration notes | Final Credential YAML |
|-----------------------|-----------------|------------------------|
| EnvGene may generate | `create: true` | `create: true` |
| Secret must already exist | `create: false` | omit `create` |
| External provider creates it | `create: false` | omit `create` |
| Unknown | do not convert yet | leave unchanged |

`create` is EnvGene runtime behaviour when the secret is absent. Separately, record whether Git
values must be copied into the Secret Store during migration:

| Migration plan field | Meaning |
|----------------------|---------|
| `writeToStore: true` | Copy actual passwords and tokens from Git to the Store, then rewrite YAML to `type: external` |
| `writeToStore: false` | Rewrite YAML only (Store already has the secret, or provider will populate) |

`writeToStore` belongs in migration notes or `migration-plan.yaml` only. It never appears in final
Credential YAML. For `writeToStore: true` entries, transfer secrets to the Secret Store before YAML
cutover (see [Transfer secrets to the Secret Store](transfer-secrets-to-store.md)).

### Classification review

Cred-ids or comments that look like platform integrations (`dbaas`, `argocd`, `arango`, `consul`,
`cluster`, `bootstrap`, and similar) need explicit review. Confirm scope before apply. When a cred
is cluster-shared, prefer passport-tier shape: path `<cluster>`, omit `create`.

### Default proposals by source (confirm before apply)

| Source | Typical path proposal | Typical create proposal |
|--------|-----------------------|-------------------------|
| Cloud Passport | `<cluster>` | omit (`false` in notes) |
| Environment-level Shared | `<cluster>/<environment>` | `true` if new values are OK |
| Cluster / repo Shared | `external` or approved shared path | omit |
| System Credentials | approved system path (fallback `external`) | always omit |

Do not set `create: true` for System Credentials or confirmed provider-managed Credentials.
Names such as `consul`, `dbaas`, or `service-account` in a `credId` are review signals only -
confirm ownership and path before converting.

**Result:** confirmed create and path decisions for every Credential you will convert.

## Step 4. Configure Secret Store

Create or update `/configuration/secret-stores.yml` with one default store:

```yaml
default_store:
  type: gcp
  projectId: <project-id>
```

Migration assumes one Secret Store per repository. Credential entries must set `secretStore`
explicitly (usually `default_store`). The Effective Set calculator has no runtime fallback.

Supported `type` values include `vault`, `gcp`, `aws`, and `azure`. Use the fields required for
the chosen type (for example `mountPath` for Vault, `region` for AWS, `vaultName` for Azure).
Configure CI/CD authentication variables for the store. See
[External Credentials Management](/docs/features/external-creds.md).

Do not store tokens or keys in Git.

**Result:** Secret Store defined.

## Step 5. Convert Cloud Passport Credentials and main file

Work through one Cloud Passport at a time: convert its `*-creds.yml` and its main file together
before moving to the next.

### Convert `*-creds.yml`

Open `environments/<cluster>/cloud-passport/*-creds.yml`.

Before:

```yaml
dbaas:
  type: usernamePassword
  data:
    username: <username>
    password: <password>
```

After (secret already in the store or provider-managed - omit `create`):

```yaml
dbaas:
  type: external
  secretStore: default_store
  remoteRefPath: <cluster>
  properties:
    - name: username
    - name: password
```

After (EnvGene generation of a new value is confirmed):

```yaml
cloud-deploy-sa-token:
  type: external
  create: true
  secretStore: default_store
  remoteRefPath: <cluster>
```

Rules:

- remove `data` only after the value is in the Store (`writeToStore: true` completed) or generation is confirmed
- always set `secretStore` (usually `default_store`)
- `properties` entries must be objects: `- name: username`, not bare strings
- do not append `credId` to `remoteRefPath` - EnvGene appends it automatically
- for Azure, AWS, or GCP, keep `credId` to at most 32 characters
- omit `create` when the secret already exists or a provider creates it
- set `create: true` only when EnvGene generation is explicitly allowed
- default path proposal is `<cluster>` - confirm before apply
- do not change built-in fields that store only a `credId` string

### Update the Cloud Passport main file

Open the matching main file (for example `environments/<cluster>/cloud-passport/cluster.yml`).

Replace every local Credential macro with `$type: credRef`. Search for:

```text
${creds.get('<credId>').username|password|secret}
${envgen.creds.get('<credId>').username|password|secret}
${cmdb.creds.get('<credId>').username|password|secret}
#creds{LOGIN_PARAM, PASSWORD_PARAM}
#credscl{LOGIN_PARAM, PASSWORD_PARAM}
#credsns{LOGIN_PARAM, PASSWORD_PARAM}
```

Before:

```yaml
dbaas:
  DBAAS_CLUSTER_DBA_CREDENTIALS_USERNAME: ${creds.get("dbaas").username}
  DBAAS_CLUSTER_DBA_CREDENTIALS_PASSWORD: ${creds.get("dbaas").password}
consul:
  CONSUL_ADMIN_TOKEN: ${creds.get("consul").secret}
```

After:

```yaml
dbaas:
  DBAAS_CLUSTER_DBA_CREDENTIALS_USERNAME:
    $type: credRef
    credId: dbaas
    property: username
  DBAAS_CLUSTER_DBA_CREDENTIALS_PASSWORD:
    $type: credRef
    credId: dbaas
    property: password
consul:
  CONSUL_ADMIN_TOKEN:
    $type: credRef
    credId: consul
```

### Legacy `#creds` / `#credscl` / `#credsns` keys

These macros sit in the parameter key. The value is the `credId`. Split each key into two
parameters with `$type: credRef`.

Before:

```yaml
'#creds{TEST_CREDS_LOGIN, TEST_CREDS_PASSWORD}': test-cred
```

After:

```yaml
TEST_CREDS_LOGIN:
  $type: credRef
  credId: test-cred
  property: username
TEST_CREDS_PASSWORD:
  $type: credRef
  credId: test-cred
  property: password
```

All three variants expand the same way.

Do not add `$type: credRef` to `technicalConfigurationParameters`.

A macro embedded inside a larger string (for example `user=${creds.get('X').username}@host`) cannot
become `$type: credRef`. Stop and split into separate parameters before continuing.

Do not edit generated files under `effective-set/` by hand.

**Result:** Cloud Passport Credentials and main files use External Credentials and Credential
References.

## Step 6. Convert Shared Credentials

Open each Shared Credential file referenced from `envTemplate.sharedMasterCredentialFiles` across
all `env_definition.yml` files. Also scan repository-level and cluster-level Shared paths from
Step 2.

Apply the same conversion rules as Step 5 for Credential YAML, using the decisions from Step 3:

- cluster / repository Shared - typical path `external` (or an approved shared path), omit `create`
- environment-level Shared - typical path `<cluster>/<environment>`, `create: true` only when new
  values are allowed

Before:

```yaml
ID_TOCP_CLIENT_CREDS:
  type: usernamePassword
  data:
    username: <username>
    password: <password>
```

After:

```yaml
ID_TOCP_CLIENT_CREDS:
  type: external
  remoteRefPath: external
  properties:
    - name: username
    - name: password
```

> [!IMPORTANT]
> `sharedMasterCredentialFiles` references the file by name **without** the `.yml` extension.
> EnvGene appends `.yml` automatically. If `env_definition.yml` has
> `"shared-credentials.yml"`, change it to `shared-credentials`. Including the extension causes
> the file to be skipped.

**Result:** Shared Credentials are External Credentials and `env_definition.yml` references are
updated where needed.

## Step 7. Convert System Credentials

System Credentials cover git tokens and registry authentication. They live in:

- `/configuration/credentials/credentials.yml` - `self_token`, `cp_discovery` token, registry

Apply the same conversion rules as Step 5 for Credential YAML, with these additional constraints:

- omit `create` - `create: true` is not allowed for System Credentials
- only Vault and GCP are supported as Secret Stores for System Credentials
- the secret must already exist in the Secret Store (from `writeToStore: true` transfer)
- use an approved system path (fallback `external`)

After converting the Credential entries, update references in `/configuration/integration.yml` and
`/configuration/registry.yml`.

`/configuration/integration.yml` - before:

```yaml
self_token: "${envgen.creds.get('self-token-cred').secret}"
cp_discovery:
  gitlab:
    token: "${envgen.creds.get('cp-discovery-repository-cred').secret}"
```

After:

```yaml
self_token:
  $type: credRef
  credId: self-token-cred
cp_discovery:
  gitlab:
    token:
      $type: credRef
      credId: cp-discovery-repository-cred
```

`credentialsId` fields in Artifact Definition and Registry Definition files stay as plain strings.

See [EnvGene System Credentials](/docs/features/external-creds.md#envgene-system-credentials).

**Result:** System Credentials converted to External Credentials.

## Step 8. Update environment-specific parameters

Replace the same macros as in Step 5 (`creds.get`, `envgen.creds.get`, `cmdb.creds.get`, `#creds`,
`#credscl`, `#credsns`) with `$type: credRef` in:

- environment-specific `deployParameters` and `e2eParameters`
- ParameterSets under `environments/<cluster>/<env>/Inventory/parameters/`,
  `environments/<cluster>/parameters/`, and `environments/parameters/`

Use the same before/after shapes as in Step 5.

Composite macros embedded in larger strings are blocked - split into separate parameters first.

Do not add `$type: credRef` to `technicalConfigurationParameters`. Do not edit `effective-set/`
files by hand.

**Result:** environment-specific parameters use Credential References.

## Step 9. Remove stale and out-of-scope Credential files

Delete generated Environment Instance credential files:

```text
environments/<cluster>/<env>/Credentials/credentials.yml
```

Delete deployer credential files (No-CMDB - out of migration scope):

```text
environments/<cluster>/app-deployer/deployer-creds.yml
```

Delete orphaned Shared Credential files identified in Step 1 (no consumer references any
`credId` from that file).

Generated files were produced by a previous pipeline run from the old Template. They contain local
`type: usernamePassword` or `type: secret` entries and cause the pipeline to fail with
`Only external credentials allowed` if they remain.

The pipeline regenerates `Credentials/credentials.yml` from migrated sources (only `type: external`
entries).

**Result:** stale generated, deployer, and orphaned Shared Credential files removed.

## Step 10. Validate before the pipeline

Check:

- all changed YAML files are syntactically valid
- every Environment Instance, when its sources are merged, produces External Credentials only
- no Credential file has a leftover `data` block for converted entries
- every converted Credential had a confirmed creation owner, path, and `writeToStore` decision
- `credRef.property` matches a name in the Credential's `properties` list
- no composite credential macros remain (macro must be the full scalar value)
- no `$type: credRef` in `technicalConfigurationParameters`
- secrets exist in the store for all entries where `create` is omitted
- no `create: false` written in YAML (omit the field instead)
- `sharedMasterCredentialFiles` entries do not have the `.yml` extension

**Result:** Instance Repository ready for the Instance pipeline.

## Step 11. Run the Instance pipeline

You run the pipeline - skills do not. Start with a non-production Environment Instance.

Typical parameters:

```text
ENV_NAMES=<environment-name>
ENV_TEMPLATE_VERSION=<artifactId>:<version>
ENV_BUILDER=true
GENERATE_EFFECTIVE_SET=true
CMDB_IMPORT=false
```

If secrets must already exist in the Store before provisioning can succeed, follow
[Transfer secrets to the Secret Store](transfer-secrets-to-store.md) between Context generation and a
final apply run.

Keep other No-CMDB parameters from your working flow. See
[Update template version](/docs/how-to/update-template-version.md) if you set the Template version
manually in `env_definition.yml`.

**Result:** Environment Instance generated with External Credentials.

## Step 12. Verify the generated result

For each Environment Instance you ran, check:

- `environments/<cluster>/<env>/Credentials/credentials.yml` - all entries are `type: external`
- `effective-set/external-credential/external-credentials.yaml` - expected Credentials listed
- `effective-set/deployment/` - deployment contexts use VALS or ESO references, not passwords or tokens

If verification fails, fix the configuration and re-run Step 11 for those environments.

**Result:** generated External Credentials validated.

## Step 13. Run a test deployment

Run a test deployment for the same environments. Confirm that applications start and that
authentication to dependent services works.

When generation and the test deployment succeed, continue with the remaining Environment Instances
the same way.

**Result:** External Credentials validated end-to-end.

## Rollback

### Before keeping the new Template version

1. keep using the previous Template version
2. revert Instance Repository changes
3. re-run the Instance pipeline on the test environments

### After keeping the new Template version

1. restore the previous Template version
2. restore consistent Instance Repository changes
3. regenerate Environment Instances and Effective Sets
4. do not delete external secrets until confirmed unused

## See also

- [Overview](overview.md)
- [Migrate Template Repository](migrate-template-repository.md)
- [Transfer secrets to the Secret Store](transfer-secrets-to-store.md)
- [Instance skill](../skills/instance-repository/SKILL.md)
- [External Credentials Management](/docs/features/external-creds.md)
- [Sample External Credentials](/docs/samples/external-credentials/)
