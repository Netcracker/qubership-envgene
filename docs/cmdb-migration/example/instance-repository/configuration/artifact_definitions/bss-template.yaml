# Artifact Definition reference:
# https://github.com/Netcracker/qubership-envgene/blob/main/docs/envgene-objects.md#artifact-definition

name: bss-template
groupId: org.qubership.bss
artifactId: bss-template
registry:
  name: artifactory
  credentialsId: artifactory-cred
  mavenConfig:
    repositoryDomainName: https://artifactory.example.com
    targetSnapshot: mvn.snapshot
    targetStaging: mvn.staging
    targetRelease: mvn.release
