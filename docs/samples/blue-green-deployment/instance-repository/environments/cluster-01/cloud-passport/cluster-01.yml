# Placeholder (structure only). Cloud Passport - cluster connection and infrastructure data,
# shared by the environments in this cluster. Normally downloaded or authored. The real file
# holds the passport. Discovered by the cluster/cloud directory name.
