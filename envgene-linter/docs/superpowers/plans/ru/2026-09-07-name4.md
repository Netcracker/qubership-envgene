# План: NAME-4

Английская копия (по ней пишут код): [../2026-09-07-name4.md](../2026-09-07-name4.md)

**Что получим:** `check` пишет NAME-4, когда привязанный стем ParameterSet не `<subject>-<хвост>` или в имени зашиты cluster/env/ticket/release. Information / Review. Файлы не переименовываем.

## Пять задач

### 1. Каталог и пустой блок
NAME-4 в `rulemeta` (Information / Review), `RULE_ORDER` из семи, точные строки «No findings».

### 2. Правило
`rules/name4.py` + `tests/test_name4.py`: хвосты deploy / pipeline / technical, один finding на стем, jinja, без непривязанных.

### 3. Engine и отчёты
`run_check` вызывает NAME-4. CLI/HTML после NAME-3; `env-params` — finding.

### 4. Algorithm-доки
`docs/algorithms/name4.md` и `ru/name4.md`. В NAME-1…NAME-3 — семь заголовков.

### 5. Весь pytest
