# План: NAME-3

Английская копия (по ней пишут код): [../2026-09-07-name3.md](../2026-09-07-name3.md)

**Что получим:** `check` находит каталоги cluster/env/namespace и стемы YAML из индекса, которые не kebab-case. Warning / Fix. Файлы не переименовываем.

## Пять задач

### 1. Каталог и пустой блок
Запись NAME-3 в `rulemeta` (Warning / Fix), `RULE_ORDER` из шести, обновить точные строки «No findings».

### 2. Правило
`rules/name3.py` + `tests/test_name3.py`: cluster, env, Namespaces/, paramset-стем, skip `env_definition` и `Inventory`.

### 3. Engine и отчёты
`run_check` вызывает NAME-3. CLI/HTML: блок после NAME-2, чипы Warning / Fix.

### 4. Algorithm-доки
`docs/algorithms/name3.md` и `ru/name3.md`. В NAME-1/NAME-2 доках — `RULE_ORDER` из шести.

### 5. Весь pytest
