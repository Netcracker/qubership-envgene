# План: PLACE-4

Английская копия (по ней пишут код): [../2026-09-07-place4.md](../2026-09-07-place4.md)

**Что получим:** `check` пишет PLACE-4, когда в привязанном ParameterSet лежит ключ из таблицы Cloud Passport. Warning / Fix. Файлы не правим. PLACE-3 остаётся только про файлы паспорта не на кластере.

## Пять задач

### 1. Каталог и пустой блок
PLACE-4 в `rulemeta` (Warning / Fix), `RULE_ORDER` из восьми (после PLACE-3, до NAME-1), точные строки «No findings».

### 2. Правило
`rules/place4.py` + `tests/test_place4.py`: только `TABLE`, только привязанные стемы, все слои, один finding на файл+ключ, jinja через `load`.

### 3. Сузить PLACE-3
Убрать проверки ключей. Лаб `place3/not-ok` — это PLACE-4. Добавить `testdata/place4/ok` без ключей таблицы.

### 4. Engine и отчёты
`run_check` вызывает PLACE-4 после PLACE-3. CLI/HTML: `warning`, заголовок и чипы Warning / Fix.

### 5. Algorithm-доки
`docs/algorithms/place4.md` и `ru/place4.md`. В PLACE-3 ключи → PLACE-4. В NAME-* — восемь заголовков.
