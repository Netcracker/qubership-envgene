# План: PLACE-6

Английская копия (по ней пишут код): [../2026-09-09-place6.md](../2026-09-09-place6.md)

**Что получим:** `check` пишет PLACE-6, когда в `envSpecificE2EParamsets` ключ не `cloud` (без регистра). Warning / Fix. Один finding на env + target. `env_definition` не правим.

## Четыре задачи

### 1. Каталог и пустой блок
PLACE-6 в `rulemeta` (Warning / Fix), `RULE_ORDER` из девяти (после PLACE-4, до NAME-1), точные строки «No findings».

### 2. Правило
`rules/place6.py` + `tests/test_place6.py`: только e2e-привязки, `cloud` без регистра ок, один finding на target.

### 3. Engine и отчёты
`run_check` вызывает PLACE-6 после PLACE-4. CLI/HTML: `warning`. Лаб `testdata/place6/`.

### 4. Algorithm-доки
`docs/algorithms/place6.md` и `ru/place6.md`. В PLACE-3/4 и NAME-* — девять заголовков, PLACE-5 нет.
