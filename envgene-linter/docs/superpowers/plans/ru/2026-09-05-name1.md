# План: NAME-1

Английская копия (по ней пишут код): [../2026-09-05-name1.md](../2026-09-05-name1.md)

**Что получим:** `check` находит разные имена ключей с одной непустой строкой. TYPE Information, ACTION Review. Не чиним.

## Пять задач

### 1. Каталог и пустой блок
`Severity.INFORMATION`, запись NAME-1 в `rulemeta`, `RULE_ORDER` из четырёх, обновить точные строки «No findings» в консольных тестах.

### 2. Правило
`rules/name1.py` + `tests/test_name1.py`: все paramset’ы, листья, группы по value, пары user/password, orphan, jinja, applications.

### 3. Engine и отчёты
`run_check` вызывает NAME-1. CLI/HTML тесты: блок после PLACE-3, чипы Information / Review.

### 4. Algorithm-доки
`docs/algorithms/name1.md` и `ru/name1.md`.

### 5. Весь pytest
