# План: NAME-2

Английская копия (по ней пишут код): [../2026-09-07-name2.md](../2026-09-07-name2.md)

**Что получим:** `check` находит ParameterSet и определения, где `name` ≠ стем файла, или `name` нет. Несовпадение — Warning / Fix. Нет имени — Information / Review. Файлы не правим.

## Пять задач

### 1. Каталог и пустой блок
Запись NAME-2 в `rulemeta` (Warning / Fix), `RULE_ORDER` из пяти, обновить точные строки «No findings».

### 2. Discovery и правило
`NamedEntityFile` + пять каталогов. `rules/name2.py` + `tests/test_name2.py`: paramset, orphan, jinja, битый YAML, нет/`""` name, artifact, application.

### 3. Engine и отчёты
`run_check` вызывает NAME-2. CLI/HTML: блок после NAME-1, чипы Warning / Fix.

### 4. Algorithm-доки
`docs/algorithms/name2.md` и `ru/name2.md`.

### 5. Весь pytest
